@model WebApplication1.Models.ViewModels.Enrollments.DisplayEnrollmentVm

@{
    ViewBag.Title = "Unenroll";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Unenroll</h2>

<h3>Are you sure you want to remove this enrollment</h3>
<div>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.PersonName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PersonName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CourseName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CourseName)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(model=>model.EnrollmentId)
        @Html.Hidden("Url",(Request?.UrlReferrer?.ToString() ?? "/Home/Index"))
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a href="@(Request?.UrlReferrer?.ToString() ?? "/Home/Index" )" class="btn btn-outline-info"> Back </a>
        </div>
    }
</div>
